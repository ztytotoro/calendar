{
  "name": "kalender",
  "workspaces": [
    "packages/core",
    "packages/ui"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "build": "lerna run build",
    "pub": "lerna run pub"
  }
}
